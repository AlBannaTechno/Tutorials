/* this program shows various ways to initialize pointers */

int main() {
    int a, b, c;
    int* d, e, f;  // only the first variable, d, will be a pointer
    int *g, *h;
    int* i, * j;

    return 0;
}
